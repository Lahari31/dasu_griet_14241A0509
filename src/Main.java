import java.util.*;
public class Main {

	public static void main(String[] args) {
BufferedReader br=new BufferedReader(new InputStreamReader(System.in));
Student st=new Student(Integer.parseInt(br.readLine(),br.readline,Integer.parseInt(br.readLine()),Date.parseDate(br.readLine));
		StudentGroup sg=new StudentGroup(15);
sg.getStudents();
sg.setStudents(br.readLine());
sg.getStudent(Integer.parseInt(br.readLine());
sg.setStudent(br.readLine(),Integer.parseInt(br.readLine());


		//You may test that your code works find here
		//Please check that your code works and has no 
		//compilation problems before to submit
	}

}
